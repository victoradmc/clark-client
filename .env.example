# Public client config — safe to expose in the browser bundle.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Used by scripts/seed-first-admin.ts and local integration tests only —
# never referenced from src/. Get these by running `supabase status` once
# the local dev stack is up (see README).
SUPABASE_LOCAL_URL=
SUPABASE_LOCAL_ANON_KEY=
SUPABASE_LOCAL_SERVICE_ROLE_KEY=
