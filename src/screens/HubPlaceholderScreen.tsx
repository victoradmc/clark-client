export default function HubPlaceholderScreen() {
  return (
    <p className="text-sm text-[#8A8D93]">
      Signed in. The full Lesson Hub is coming in a later build — use
      "Upload Lesson" above to publish one.
    </p>
  );
}
